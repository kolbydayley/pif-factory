#!/usr/bin/env python3
"""Render Signal Desk as a self-contained, deep-linkable research app."""

import json
from pathlib import Path


PIF_ROOT = Path.home() / "pif-factory"
DATA = PIF_ROOT / "work" / "pif-ops" / "dashboard" / "data.json"
OUT = PIF_ROOT / "work" / "pif-ops" / "dashboard" / "dashboard.html"
FUNNEL_OUT = OUT.with_name("pif-signal-desk-funnel.json")


TEMPLATE = r"""<!doctype html>
<html lang="en"><head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<meta name="theme-color" content="#faf8f3">
<title>Signal Desk</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,500;9..144,700;9..144,900&family=IBM+Plex+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
:root{--paper:#faf8f3;--panel:#fff;--ink:#171614;--ink2:#57544d;--ink3:#817d73;--rule:#e4e0d5;--rule2:#d3cec0;--blue:#2a78d6;--red:#d84b4a;--green:#178b62;--gold:#a56b00;--violet:#5540a6;--neutral:#b9b4a8;--serif:"Fraunces",Georgia,serif;--sans:"IBM Plex Sans",-apple-system,sans-serif;--mono:"IBM Plex Mono",ui-monospace,monospace;--shadow:0 12px 32px rgba(23,22,20,.08)}
*{box-sizing:border-box}html{color-scheme:light;scroll-behavior:smooth}body{margin:0;overflow-x:hidden;background:var(--paper);color:var(--ink);font:15px/1.5 var(--sans);background-image:radial-gradient(rgba(23,22,20,.025) 1px,transparent 1px);background-size:26px 26px}button,input,select,a{font:inherit}button{color:inherit}a{color:inherit}button:focus-visible,a:focus-visible,input:focus-visible,select:focus-visible{outline:3px solid rgba(42,120,214,.35);outline-offset:3px}#app:focus{outline:none}.shell{max-width:1180px;margin:auto;padding:0 28px 90px}
.topbar{min-height:70px;display:flex;align-items:center;justify-content:space-between;gap:20px;border-bottom:1px solid var(--rule2);background:rgba(250,248,243,.96);position:sticky;top:0;z-index:20}.brand{border:0;background:none;padding:8px 0;cursor:pointer;text-align:left}.brand strong{font:900 26px/1 var(--serif)}.brand span{font:11px var(--mono);color:var(--ink3);margin-left:10px}.desktop-nav{display:flex;gap:4px}.nav-link{border:0;background:transparent;border-radius:999px;padding:10px 14px;color:var(--ink2);cursor:pointer;font-weight:600}.nav-link.active{background:var(--ink);color:var(--paper)}.bottom-nav{display:none}.view{padding-top:34px}.eyebrow{font:500 11px var(--mono);letter-spacing:.14em;text-transform:uppercase;color:var(--ink3)}
h1,h2,h3,p{margin:0}h1{font:900 clamp(36px,6vw,68px)/.98 var(--serif);letter-spacing:-.025em}h2{font:700 clamp(25px,3vw,36px)/1.08 var(--serif)}h3{font:700 20px/1.15 var(--serif)}.lede{max-width:720px;margin-top:12px;color:var(--ink2);font-size:17px}.title-case{text-transform:capitalize}.home-head{display:grid;grid-template-columns:1fr auto;gap:24px;align-items:end;margin-bottom:26px}.status{border:1px solid #e3c276;background:#fff9e9;color:#775000;border-radius:999px;padding:7px 11px;font:11px var(--mono);max-width:330px}.section-head{display:flex;align-items:end;justify-content:space-between;gap:20px;border-bottom:2px solid var(--ink);padding-bottom:10px;margin:34px 0 16px}.section-head p{font:12px var(--mono);color:var(--ink3)}
.signal-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.signal-card,.person-card,.topic-card,.issue-card{width:100%;text-align:left;background:var(--panel);border:1px solid var(--rule);border-radius:14px;cursor:pointer;transition:.15s transform,.15s border-color,.15s box-shadow}.signal-card{padding:18px;min-height:182px;display:grid;grid-template-rows:auto auto 1fr auto}.signal-card:hover,.person-card:hover,.topic-card:hover,.issue-card:hover{transform:translateY(-2px);border-color:var(--rule2);box-shadow:var(--shadow)}.signal-kind{font:500 11px var(--mono);letter-spacing:.12em;text-transform:uppercase}.signal-card h3{font-size:24px;margin:9px 0 5px;text-transform:capitalize}.signal-card p{color:var(--ink2)}.card-foot{display:flex;align-items:end;justify-content:space-between;gap:18px;margin-top:16px}.explore{font:600 12px var(--sans);color:var(--blue)}.spark{height:44px;width:150px;display:flex;align-items:flex-end;gap:3px}.spark i{display:block;flex:1;min-width:2px;background:currentColor;border-radius:3px 3px 1px 1px;opacity:.82}
.browse-grid{display:grid;grid-template-columns:1fr 1fr;gap:30px}.list{display:grid;gap:10px}.person-card,.topic-card{padding:15px 16px}.row-top{display:flex;justify-content:space-between;gap:12px;align-items:start}.row-title{font:700 19px/1.2 var(--serif);text-transform:capitalize}.meta{font:11px var(--mono);color:var(--ink3)}.pill-row{display:flex;gap:6px;flex-wrap:wrap;margin-top:10px}.pill{border:1px solid var(--rule2);border-radius:999px;padding:4px 8px;font:11px var(--mono);color:var(--ink2);background:var(--paper)}.pill.trust{border-color:#b9dccf;background:#eef9f4;color:#155c43}.pill.shift{border-color:#d7c7e4;background:#f7f1fb;color:#6e3f88}.search{width:100%;min-height:46px;border:1px solid var(--rule2);border-radius:10px;background:var(--panel);padding:10px 13px;margin-bottom:12px;color:var(--ink)}
.back{border:0;background:transparent;color:var(--blue);font-weight:600;min-height:44px;padding:0;cursor:pointer}.detail-head{display:grid;grid-template-columns:1fr auto;gap:24px;align-items:end;margin:10px 0 24px}.detail-head h1{font-size:clamp(38px,7vw,72px);text-transform:capitalize}.detail-label{color:var(--green);margin-bottom:8px}.metric-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.metric{background:var(--panel);border:1px solid var(--rule);border-radius:12px;padding:14px}.metric b{font:700 24px var(--serif);display:block}.metric span{font:11px var(--mono);color:var(--ink3)}.research-grid{display:grid;grid-template-columns:minmax(0,1.4fr) minmax(290px,.6fr);gap:24px;align-items:start;margin-top:24px}.research-grid>*{min-width:0}.panel{min-width:0;background:var(--panel);border:1px solid var(--rule);border-radius:16px;padding:20px}.panel+.panel{margin-top:16px}.panel-title{font:700 22px var(--serif);margin-bottom:4px}.panel-sub{color:var(--ink2);font-size:13px;margin-bottom:16px}
.trend-scroll{overflow-x:auto;padding:6px 2px 10px}.trend{height:150px;min-width:650px;display:flex;align-items:end;gap:5px;border-bottom:1px solid var(--rule2);padding:0 2px}.week-bar{width:100%;height:140px;min-width:18px;border:0;background:transparent;display:flex;flex-direction:column;justify-content:flex-end;padding:0;cursor:pointer}.week-bar.low-sample{opacity:.34}.week-bar .bar{display:flex;flex-direction:column-reverse;width:100%;min-height:2px;border-radius:4px 4px 1px 1px;overflow:hidden}.week-bar .bar i{display:block;width:100%}.week-bar:hover .bar,.week-bar.active .bar{box-shadow:0 0 0 2px var(--ink)}.week-label{font:9px var(--mono);color:var(--ink3);margin-top:5px}.filter-row{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.filter{min-height:42px;border:1px solid var(--rule2);background:var(--paper);border-radius:999px;padding:7px 11px;cursor:pointer}.filter.active{background:var(--ink);color:var(--paper)}.tier-badge{display:inline-block;font:10px var(--mono);letter-spacing:.08em;text-transform:uppercase;border-radius:999px;padding:2px 8px;margin-left:8px;vertical-align:middle}.tier-badge.strong{background:var(--ink);color:var(--paper)}.tier-badge.moderate{border:1px solid var(--ink);color:var(--ink)}.tier-badge.weak{border:1px dashed var(--ink3);color:var(--ink3)}.signal-card.strong{border-left:4px solid var(--ink)}.signal-card.weak{filter:grayscale(1);opacity:.7}.diff-log ul{margin:8px 0 0;padding-left:18px}.diff-log li{margin:4px 0;font-size:14px}.ribbon-legend{font:11px var(--mono);color:var(--ink3);margin:4px 0 12px;line-height:1.5}.ask-box{display:flex;gap:8px;margin:14px 0;align-items:stretch}.ask-box .search{flex:1}.cmp-row{display:flex;align-items:center;gap:10px;margin:8px 0;flex-wrap:wrap}.cmp-bar{min-width:160px}.cmp-label{font:12px var(--mono);color:var(--ink3);width:86px;flex:none}.cmp-bar{flex:1;display:flex;height:22px;border-radius:6px;overflow:hidden;background:var(--paper);border:1px solid var(--rule2)}.cmp-bar i{display:block;height:100%}.cmp-nums{font:11px var(--mono);color:var(--ink3);flex:none}.ctx-where{font:13px var(--mono);color:var(--ink3);margin:10px 0 16px;line-height:1.6}.ctx-quote{font-size:17px;line-height:1.75;margin:8px 0 6px;max-width:70ch}.ctx-dim{color:var(--ink3)}.ctx-mark{background:transparent;color:var(--ink);font-weight:600;border-bottom:2px solid var(--blue);padding:0 1px}.ctx-long{display:none}.ctx-open .ctx-long{display:inline}.ctx-open .ctx-short{display:none}.ctx-toggle{margin:6px 0 2px}
.issue-grid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.issue-card{padding:12px}.issue-card b{display:block;text-transform:capitalize}.issue-card span{font:11px var(--mono);color:var(--ink3)}.callout{border-left:4px solid var(--gold);background:#fff9e9;padding:12px 14px;border-radius:8px;color:#6f4c08;font-size:13px}.evidence-list{display:grid;gap:12px}.evidence{border:1px solid var(--rule);border-radius:14px;background:var(--panel);padding:16px}.evidence-top{display:flex;justify-content:space-between;gap:12px;align-items:start}.person-link{border:0;background:none;padding:0;cursor:pointer;text-align:left;font-weight:700}.quote{font:500 18px/1.42 var(--serif);margin:13px 0;color:#292722}.source-line{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px 16px;align-items:end;color:var(--ink3);font:11px/1.55 var(--mono)}.source-title{min-width:0}.source-actions{display:flex;align-items:center;justify-content:flex-end;gap:14px;flex-wrap:wrap}.source-actions .back{min-height:36px}.source-line a{color:var(--blue);font-weight:600;text-decoration:none;min-height:36px;display:inline-flex;align-items:center}.stance{border-radius:999px;padding:4px 8px;font:10px var(--mono);text-transform:uppercase}.stance.positive{background:#eaf3fe;color:#175aab}.stance.negative{background:#fff0ef;color:#a12c2c}.stance.neutral{background:#f0eee8;color:#625e56}
.claim-list{display:grid;gap:9px}.claim{border:1px solid var(--rule);border-radius:12px;padding:13px;background:var(--panel);cursor:pointer;text-align:left}.claim b{display:block;text-transform:capitalize}.claim .stance-line{display:flex;height:5px;border-radius:999px;overflow:hidden;margin-top:9px;background:var(--rule)}.stance-line i{display:block}.empty{padding:24px;border:1px dashed var(--rule2);border-radius:12px;color:var(--ink2)}.evidence-page{max-width:760px;margin:auto}.evidence-page blockquote{font:500 clamp(28px,5vw,46px)/1.25 var(--serif);margin:22px 0}.source-card{background:var(--panel);border:1px solid var(--rule);border-radius:14px;padding:18px}.button-row{display:flex;gap:10px;flex-wrap:wrap;margin-top:20px}.primary,.secondary{min-height:46px;border-radius:10px;padding:10px 14px;cursor:pointer;font-weight:600}.primary{border:1px solid var(--ink);background:var(--ink);color:var(--paper)}.secondary{border:1px solid var(--rule2);background:var(--panel)}.coverage{margin-top:42px;padding-top:16px;border-top:3px double var(--rule2);color:var(--ink3);font:11px/1.7 var(--mono)}
.funnel-grid{display:grid;gap:9px;margin:26px 0}.funnel-stage{margin-inline:auto;background:var(--panel);border:1px solid var(--rule2);border-radius:14px;padding:14px 16px;min-width:36%;box-shadow:0 4px 14px rgba(23,22,20,.04)}.funnel-stage-top{display:flex;align-items:start;justify-content:space-between;gap:16px}.funnel-stage h3{font-size:19px}.funnel-stage .meta{display:block;margin-top:7px}.funnel-stage-metrics{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:12px}.funnel-stage-metrics span{min-width:0;border-left:1px solid var(--rule);padding-left:9px}.funnel-stage-metrics span:first-child{border-left:0;padding-left:0}.funnel-stage-metrics b{display:block;font:700 23px/1 var(--serif);white-space:nowrap}.funnel-stage-metrics small{display:block;margin-top:4px;color:var(--ink3);font:10px var(--mono);text-transform:uppercase}.funnel-conversion{height:5px;border-radius:999px;background:var(--rule);overflow:hidden;margin-top:11px}.funnel-conversion i{display:block;height:100%;background:var(--blue)}.funnel-layout{grid-template-columns:minmax(0,1.35fr) minmax(310px,.65fr)}.roster-controls{display:grid;grid-template-columns:minmax(0,1fr) 180px;gap:8px}.roster-controls .search{margin:0}.roster-select,.form-field input{width:100%;min-height:46px;border:1px solid var(--rule2);border-radius:10px;background:var(--panel);padding:10px 13px;color:var(--ink)}.show-list{display:grid;gap:9px;margin-top:12px}.show-row{background:var(--panel);border:1px solid var(--rule);border-radius:12px;padding:13px 14px}.show-row h3{font-size:18px}.show-row-top{display:flex;align-items:start;justify-content:space-between;gap:12px}.show-row a{color:var(--blue);font:600 12px var(--sans);text-decoration:none;min-height:36px;display:inline-flex;align-items:center}.show-counts{font:11px/1.65 var(--mono);color:var(--ink3);margin-top:8px}.enroll-form{display:grid;gap:13px}.form-field{display:grid;gap:5px}.form-field label{font:600 12px var(--sans);color:var(--ink2)}.form-actions{display:grid;gap:8px}.enroll-note{min-height:20px;color:var(--ink3);font:11px/1.55 var(--mono)}
@media(max-width:820px){.shell{padding:0 16px calc(92px + env(safe-area-inset-bottom))}.topbar{min-height:58px}.brand strong{font-size:22px}.brand span{display:none}.desktop-nav{display:none}.bottom-nav{display:grid;grid-template-columns:repeat(4,1fr);position:fixed;left:12px;right:12px;bottom:calc(10px + env(safe-area-inset-bottom));z-index:30;background:rgba(255,255,255,.96);border:1px solid var(--rule2);border-radius:16px;padding:5px;box-shadow:0 12px 38px rgba(23,22,20,.18)}.bottom-nav .nav-link{min-height:48px;padding:7px 3px;font-size:11px}.view{padding-top:22px}.home-head{display:block;margin-bottom:18px}.home-head h1{font-size:42px}.lede{font-size:15px}.status{margin-top:14px;max-width:none}.signal-grid{grid-template-columns:1fr}.signal-card{min-height:156px;padding:16px}.signal-card h3{font-size:23px}.spark{width:120px}.browse-grid{grid-template-columns:1fr}.section-head{margin-top:28px}.section-head p{display:none}.detail-head{display:block}.detail-head h1{font-size:42px}.metric-grid{grid-template-columns:repeat(3,1fr)}.metric{padding:10px}.metric b{font-size:20px}.research-grid{grid-template-columns:minmax(0,1fr)}.topic-layout>aside,.funnel-layout>aside{order:-1}.panel{padding:16px}.issue-grid{grid-template-columns:1fr}.source-line{grid-template-columns:1fr}.source-actions{justify-content:flex-start}.evidence-page blockquote{font-size:30px}.quote{font-size:17px}.funnel-stage{width:100%!important;min-width:100%;padding:13px}.funnel-stage strong{font-size:21px}.roster-controls{grid-template-columns:1fr}.show-row-top{display:block}.show-row a{margin-top:5px}}
@media(max-width:420px){.detail-head h1{font-size:38px}.metric-grid{grid-template-columns:1fr}.metric{display:flex;justify-content:space-between;align-items:center}.metric b{order:2}.card-foot{align-items:center}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}
</style></head><body>
<div class="shell"><header class="topbar"><button class="brand" data-route="home" data-id="shifts"><strong>Signal Desk</strong><span>podcast discourse</span></button><nav class="desktop-nav" aria-label="Primary"><button class="nav-link" data-route="home" data-id="shifts">Shifts</button><button class="nav-link" data-route="home" data-id="people">People</button><button class="nav-link" data-route="home" data-id="topics">Topics</button><button class="nav-link" data-route="funnel" data-nav="funnel">Podcast Funnel</button></nav></header><main id="app" tabindex="-1"></main><nav class="bottom-nav" aria-label="Mobile primary"><button class="nav-link" data-route="home" data-id="shifts">Shifts</button><button class="nav-link" data-route="home" data-id="people">People</button><button class="nav-link" data-route="home" data-id="topics">Topics</button><button class="nav-link" data-route="funnel" data-nav="funnel">Funnel</button></nav></div>
<script>
const DATA=__DATA__;const app=document.querySelector("#app");const esc=s=>String(s??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));const cap=s=>String(s??"").replace(/\b\w/g,c=>c.toUpperCase());const enc=s=>encodeURIComponent(String(s));const safeUrl=u=>/^https?:\/\//i.test(u||"")?u:null;const peopleByName=new Map(DATA.people.map(p=>[p.name,p]));const detectorKind=new Map();for(const kind of ["emerging","shifting","contested","fading"])for(const item of DATA.detectors[kind]||[])detectorKind.set(item.topic,{kind,item});const KIND={emerging:{label:"Emerging",color:"var(--green)"},shifting:{label:"Opinion shift",color:"var(--violet)"},contested:{label:"Contested",color:"var(--gold)"},fading:{label:"Fading",color:"var(--red)"},discussed:{label:"Most discussed",color:"var(--blue)"}};const FUNNEL_DATA_URL=new URL("./pif-signal-desk-funnel.json",location.href);let liveFunnelPayload=null,funnelRefreshTimer=null,funnelLoadToken=0;
function route(kind,id="",tail=[]){location.hash=[kind,enc(id),...tail.map(enc)].filter(Boolean).join("/")}function parseRoute(){const p=(location.hash.slice(1)||"home/shifts").split("/").map(decodeURIComponent);return{kind:p[0]||"home",id:p[1]||"",filterType:p[2]||"",filterValue:p[3]||""}}function freshness(){return Math.round((Date.now()-new Date(DATA.data_through))/864e5)}
function signalCards(){const cards=[],seen=new Set(),quota={emerging:2,shifting:3,contested:2,fading:1};for(const kind of ["emerging","shifting","contested","fading"])for(const item of (DATA.detectors[kind]||[]).filter(x=>(x.tier)!=="weak").slice(0,quota[kind])){cards.push({kind,item});seen.add(item.topic)}const broad=Object.entries(DATA.topics).filter(([t,d])=>t!=="other"&&!seen.has(t)&&(d.pulse_shows||0)>=3).sort((a,b)=>(b[1].pulse_shows||0)-(a[1].pulse_shows||0)||(b[1].pulse_vol||0)-(a[1].pulse_vol||0)).slice(0,Math.max(0,8-cards.length));for(const [topic,d] of broad)cards.push({kind:"discussed",item:{topic,pulse_vol:d.pulse_vol,shows:d.pulse_shows,episodes:d.pulse_episodes}});return cards.slice(0,8)}
function signalCopy(kind,item){if(kind==="emerging")return `${item.pulse_vol} mentions across ${item.episodes} episodes and ${item.shows} shows after a near-zero baseline.`;if(kind==="shifting"){const L=["positive","negative","neutral"],dm=item.now.map((v,i)=>v-item.before[i]);let bi=0;for(let i=1;i<3;i++)if(Math.abs(dm[i])>Math.abs(dm[bi]))bi=i;const dirw=dm[bi]>0?"rose":"fell";return `${cap(L[bi])} ${dirw} ${Math.round(item.before[bi]*100)}% → ${Math.round(item.now[bi]*100)}% of positions (overall mix moved ${Math.round(item.divergence*100)}%).`}if(kind==="contested")return `${item.positive} positions for and ${item.negative} against across ${item.shows} shows.`;if(kind==="fading")return `Previously peaked at ${item.peak_week_vol} mentions per week; now ${item.pulse_vol} in four weeks.`;return `${item.pulse_vol} mentions across ${item.shows} shows and ${item.episodes} episodes in four weeks.`}function spark(series,color){const points=series.slice(-12),vals=points.map(x=>x.share_smooth??x.share??0),mx=Math.max(...vals,.0001);return `<span class="spark" style="color:${color}">${points.map((p,i)=>`<i style="height:${Math.max(3,Math.round(vals[i]/mx*42))}px;opacity:${p.low_sample?.28:.82}"></i>`).join("")}</span>`}const ASK_STOP=new Set(["the","and","for","what","who","how","does","about","with","that","this","are","was","have","has","from","will","would","should","could","why","when","where","which","their","there","been","being","them","they","its","into","than","then","some","any","all","can","say","says","said","most","more","less","very","just","like","over","under","between"]);
function askTokens(q){return String(q||"").toLowerCase().replace(/[^a-z0-9\s]/g," ").split(/\s+/).filter(w=>w.length>2&&!ASK_STOP.has(w))}
function askScore(tokens,text){if(!tokens.length)return 0;const t=String(text||"").toLowerCase();let hit=0;for(const w of tokens)if(t.includes(w))hit++;return hit/tokens.length}
function askSearch(q){const tokens=askTokens(q);if(!tokens.length)return{topics:[],people:[],evidence:[]};const topics=Object.entries(DATA.topics).map(([name,d])=>({name,d,s:askScore(tokens,name+" "+(d.related||[]).map(r=>r.topic).join(" "))})).filter(x=>x.s>=0.5).sort((a,b)=>b.s-a.s||b.d.total-a.d.total).slice(0,3);const people=DATA.people.map(p=>({p,s:askScore(tokens,p.name+" "+(p.top_topics||[]).map(t=>t.topic).join(" ")+" "+(p.shows||[]).join(" "))})).filter(x=>x.s>=0.5).sort((a,b)=>b.s-a.s).slice(0,3);const evidence=[];for(const [name,d] of Object.entries(DATA.topics)){for(const e of (d.evidence||[])){const s=askScore(tokens,name+" "+(e.person||"")+" "+(e.evidence||""));if(s>=0.6)evidence.push({e,topic:name,s})}}evidence.sort((a,b)=>b.s-a.s||(b.e.authority||0)-(a.e.authority||0));return{topics,people,evidence:evidence.slice(0,6)}}
function copyAskPrompt(){const q=document.getElementById("ask-q")?.textContent||"";const res=askSearch(q);const lines=[`Question: ${q}`,"","Podcast-discourse evidence (Signal Desk, extractive):"];for(const {e,topic} of res.evidence)lines.push(`- [${topic}] ${e.person||"unknown"} (${e.group||e.stance||"?"}, ${e.date||""}, ${e.show||""}): "${e.evidence}"`);for(const t of res.topics)lines.push(`- Topic ${t.name}: ${t.d.total} mentions in 26 weeks, ${t.d.pulse_shows||0} recent shows`);lines.push("","Answer using only this evidence; say plainly what it cannot support.");(navigator.clipboard?navigator.clipboard.writeText(lines.join("\n")):Promise.reject()).then(()=>{const b=document.getElementById("ask-copy");if(b){b.textContent="Copied";setTimeout(()=>{b.textContent="Copy prompt for an AI assistant"},1500)}}).catch(()=>{})}
function wireAsk(){const f=document.getElementById("ask-form");if(f)f.addEventListener("submit",e=>{e.preventDefault();const v=document.getElementById("ask-input").value.trim();if(v)location.hash="ask/"+enc(v)})}
function askBox(q){return `<form id="ask-form" class="ask-box" role="search"><input id="ask-input" class="search" style="margin:0" aria-label="Ask the corpus" placeholder="Ask the corpus: who is credible on agents? what changed on AGI?" value="${esc(q||"")}"><button type="submit" class="filter">Ask</button></form>`}
function renderAsk(q){navState("");const res=askSearch(q);const none=!res.topics.length&&!res.people.length&&!res.evidence.length;app.innerHTML=`<section class="view"><button class="back" data-route="home" data-id="shifts">Back to shifts</button><div class="detail-head"><div><div class="eyebrow">Ask the corpus</div><h1 id="ask-q">${esc(q)}</h1><p class="lede">Extractive answers only — tracked topics, tracked voices, linked evidence. This desk covers ${Object.keys(DATA.topics).length} topics and ${DATA.people.length} people across ${DATA.corpus.shows} shows; it never generates claims.</p></div></div>${askBox(q)}${none?`<div class="callout">The corpus cannot answer this — no tracked topic, voice, or excerpt matches those words. Try different words, or browse <a href="#home/topics">all topics</a>.</div>`:`${res.topics.length?`<section class="panel"><h2 class="panel-title">Matching topics</h2><div class="list">${res.topics.map(t=>topicCard([t.name,t.d])).join("")}</div></section>`:""}${res.people.length?`<section class="panel"><h2 class="panel-title">Matching voices</h2><div class="list">${res.people.map(x=>personCard(x.p)).join("")}</div></section>`:""}${res.evidence.length?`<section class="panel"><h2 class="panel-title">Evidence</h2><div class="evidence-list">${res.evidence.map(({e,topic})=>evidenceCard(e,topic)).join("")}</div></section>`:""}<button class="filter" id="ask-copy" onclick="copyAskPrompt()">Copy prompt for an AI assistant</button>`}${coverageNote()}</section>`;wireAsk()}
function netLayout(nodes,edges,W,H){const n=nodes.length;if(!n)return [];const order=[...nodes.keys()].sort((a,b)=>nodes[b].score-nodes[a].score);const idx=new Map(nodes.map((d,i)=>[d.name,i]));const nbrs=nodes.map(()=>[]);for(const e of edges){const a=idx.get(e.a),b=idx.get(e.b);if(a==null||b==null)continue;nbrs[a].push([b,e.w]);nbrs[b].push([a,e.w])}const ring=new Array(n),slot=new Array(n),ringCounts=[];let ri=0,left=n-0;let k=0;const caps=[1,6,10,14,18,22,26,30];for(const cap of caps){ringCounts.push(Math.min(cap,n-k));k+=cap;if(k>=n)break}while(k<n){ringCounts.push(Math.min(30,n-k));k+=30}let oi=0;for(let r=0;r<ringCounts.length;r++){for(let sIdx=0;sIdx<ringCounts[r];sIdx++){const node=order[oi++];ring[node]=r;slot[node]=sIdx}}const maxR=ringCounts.length-1||1;const RAD=r=>r===0?0:60+(Math.min(W,H)/2-70)*r/maxR;const ang=new Array(n);for(let i2=0;i2<n;i2++){const r=ring[i2],cnt=ringCounts[r];ang[i2]=2*Math.PI*slot[i2]/Math.max(cnt,1)+r*0.7}for(let pass=0;pass<3;pass++){for(const i2 of order){if(ring[i2]===0||!nbrs[i2].length)continue;let sx=0,sy=0;for(const [j2,w] of nbrs[i2]){sx+=Math.cos(ang[j2])*w;sy+=Math.sin(ang[j2])*w}if(sx||sy)ang[i2]=Math.atan2(sy,sx)+0.13*((i2*2654435761%97)/97-0.5)}for(let r=1;r<ringCounts.length;r++){const members=[...Array(n).keys()].filter(i2=>ring[i2]===r).sort((a,b)=>ang[a]-ang[b]);const minGap=2*Math.PI/Math.max(members.length,1)*0.72;for(let m=1;m<members.length;m++){if(ang[members[m]]-ang[members[m-1]]<minGap)ang[members[m]]=ang[members[m-1]]+minGap}}}return nodes.map((d,i2)=>({...d,x:W/2+RAD(ring[i2])*Math.cos(ang[i2]),y:H/2+RAD(ring[i2])*Math.sin(ang[i2])*(H/W)}))}
function renderNetwork(){navState("people");const NET=DATA.network||{nodes:[],edges:[]};const W=940,H=680;const placed=netLayout(NET.nodes,NET.edges,W,H);const pos=new Map(placed.map(d=>[d.name,d]));const maxW=Math.max(...NET.edges.map(e=>e.w),1);const edgesSvg=NET.edges.map(e=>{const a=pos.get(e.a),b=pos.get(e.b);if(!a||!b)return "";return `<line x1="${a.x.toFixed(1)}" y1="${a.y.toFixed(1)}" x2="${b.x.toFixed(1)}" y2="${b.y.toFixed(1)}" stroke="var(--ink3)" stroke-width="${(0.5+2.5*Math.sqrt(e.w/maxW)).toFixed(2)}" opacity="${(0.12+0.3*e.w/maxW).toFixed(2)}"/>`}).join("");const maxS=Math.max(...placed.map(d=>d.score),1);const labelCut=[...placed].sort((x,y)=>y.score-x.score)[Math.min(24,placed.length-1)]?.score??0;const nodesSvg=placed.map(d=>{const r=4+13*Math.sqrt(d.score/maxS);const fill=d.tier==="hub"?"var(--blue)":d.tier==="connected"?"var(--green)":"var(--neutral)";const lbl=d.score>=labelCut&&d.score>0?`<text x="${d.x.toFixed(1)}" y="${(d.y-r-4).toFixed(1)}" text-anchor="middle" font-size="10.5" fill="var(--ink)" style="font-family:var(--mono);paint-order:stroke;stroke:var(--paper);stroke-width:3px">${esc(d.name)}</text>`:"";return `<g class="netNode" data-route="person" data-id="${esc(d.name)}" style="cursor:pointer"><circle cx="${d.x.toFixed(1)}" cy="${d.y.toFixed(1)}" r="${r.toFixed(1)}" fill="${fill}" opacity="${d.tier==="peripheral"?0.55:0.9}" stroke="var(--paper)" stroke-width="1.5"><title>${esc(d.name)} — net ${Math.round(d.score)}/100 · ${d.n_links} links · ${d.n_episodes} episodes · ${d.shows} shows (${esc(d.tier)})</title></circle>${lbl}</g>`}).join("");app.innerHTML=`<section class="view"><button class="back" data-route="home" data-id="people">Back to people</button><div class="detail-head"><div><div class="eyebrow">People network</div><h1>Who appears with whom</h1><p class="lede">Every line is a shared episode; thicker lines mean more of them. Node size is network trust (PageRank over co-appearances — many relationships, weighted by how connected those relationships are). Blue hubs, green connected, gray peripheral. Tap any node to open the person.</p></div></div><div class="panel" style="overflow-x:auto"><svg viewBox="0 0 ${W} ${H}" role="img" aria-label="Co-appearance network of tracked people" style="width:100%;min-width:640px;display:block">${edgesSvg}${nodesSvg}</svg></div>${coverageNote()}</section>`;requestAnimationFrame(()=>{const sc=app.querySelector('.panel');if(sc&&sc.scrollWidth>sc.clientWidth)sc.scrollLeft=(sc.scrollWidth-sc.clientWidth)/2})}
const ACRO=new Set(["ai","agi","llm","llms","gpt","api","aws","ipo","rss","gpu","gpus","us"]);
function tTitle(t){return String(t||"").split(" ").map(w=>ACRO.has(w)?w.toUpperCase():w.charAt(0).toUpperCase()+w.slice(1)).join(" ")}
const tailW=(s,n)=>s.length<=n?s:s.slice(s.indexOf(" ",s.length-n)+1);
const headW=(s,n)=>s.length<=n?s:s.slice(0,s.lastIndexOf(" ",n));
function toggleCtx(){const q=document.getElementById("ctxq");if(!q)return;q.classList.toggle("ctx-open");const b=document.querySelector(".ctx-toggle");if(b)b.textContent=q.classList.contains("ctx-open")?"Show less context":"Show more context"}
function diffLog(){const D=DATA.diff;if(!D)return "";const items=[];for(const c of (D.detectors||[]).slice(0,8)){if(c.change==="new")items.push(`<b>${esc(c.topic)}</b> newly ${c.family}${c.tier?` (${c.tier})`:""}`);else if(c.change==="gone")items.push(`<b>${esc(c.topic)}</b> no longer ${c.family}`);else items.push(`<b>${esc(c.topic)}</b> ${c.family} ${c.from} &rarr; ${c.to}`)}for(const m of (D.movers||[]).slice(0,4))items.push(`<b>${esc(m.topic)}</b> share of discourse ${m.delta>0?"+":""}${(m.delta*100).toFixed(1)}pp`);for(const n of (D.new_people||[]).slice(0,4))items.push(`new voice: <b>${esc(n)}</b>`);if(!items.length)return "";return `<section class="panel diff-log"><h2 class="panel-title">Since ${esc(D.since)}</h2><ul>${items.map(i=>`<li>${i}</li>`).join("")}</ul></section>`}
function stanceBar(shares,label,counts){const seg=(v,c)=>v>0?`<i style="width:${(v*100).toFixed(1)}%;background:${c}"></i>`:"";return `<div class="cmp-row"><span class="cmp-label">${esc(label)}</span><span class="cmp-bar">${seg(shares[0],"var(--blue)")}${seg(shares[2],"var(--neutral)")}${seg(shares[1],"var(--red)")}</span><span class="cmp-nums">${Math.round(shares[0]*100)}% pos · ${Math.round(shares[2]*100)}% neutral · ${Math.round(shares[1]*100)}% neg${counts?` · n=${counts}`:""}</span></div>`}
function shiftCompare(sig,d){if(sig.kind!=="shifting"||!sig.item||!sig.item.now)return "";const S=d.series,pulse=S.slice(-4),base=S.slice(0,-4);const np=pulse.reduce((a,s)=>a+s.vol,0),nb=base.reduce((a,s)=>a+s.vol,0);return `<section class="panel"><h2 class="panel-title">What the shift signal compares</h2><p class="panel-sub">The detector pools the last four weeks against the 22 weeks before them — neutral counts too. The weekly chart below is too thin week-by-week to show this; the pools are what the p-value is about.</p>${stanceBar(sig.item.before,"Before",nb)}${stanceBar(sig.item.now,"Last 4 weeks",np)}</section>`}
function ribbon(topicName,d){const S=d.series;if(!S||!S.length)return "";const W=650,H=110,n=S.length,xw=W/Math.max(n-1,1),xs=i=>i*xw;const sh=S.map(s=>{const v=Math.max(s.vol,1);return s.vol?{p:s.pos/v,ng:s.neg/v,ci:s.pos_ci||null,low:s.low_sample||s.thin}:{p:0,ng:0,ci:null,low:true}});const posPts=sh.map((s,i)=>`${xs(i).toFixed(1)},${(H-s.p*H).toFixed(1)}`).join(" ");const negPts=sh.map((s,i)=>`${xs(i).toFixed(1)},${(s.ng*H).toFixed(1)}`).join(" ");let ci="";if(sh.some(s=>s.ci)){const up=sh.map((s,i)=>`${xs(i).toFixed(1)},${(H-(s.ci?s.ci[1]:s.p)*H).toFixed(1)}`).join(" ");const dnArr=sh.map((s,i)=>`${xs(i).toFixed(1)},${(H-(s.ci?s.ci[0]:s.p)*H).toFixed(1)}`).reverse().join(" ");ci=`<polygon points="${up} ${dnArr}" fill="var(--blue)" opacity=".18"/>`}const low=sh.map((s,i)=>s.low?`<rect x="${(xs(i)-xw/2).toFixed(1)}" y="0" width="${xw.toFixed(1)}" height="${H}" fill="var(--paper)" opacity=".55"/>`:"").join("");const marks=(d.inflections||[]).map(f=>{const i=S.findIndex(s=>s.week===f.week);if(i<0)return "";const g=f.kind==="surge"?"▲":f.kind==="fade"?"▼":"⇄";return `<g data-route="topic" data-id="${esc(topicName)}" data-tail="week/${esc(f.week)}" style="cursor:pointer"><line x1="${xs(i).toFixed(1)}" x2="${xs(i).toFixed(1)}" y1="0" y2="${H}" stroke="var(--ink)" stroke-dasharray="3 3" opacity=".55"/><text x="${xs(i).toFixed(1)}" y="13" text-anchor="middle" font-size="11" fill="var(--ink)">${g}<title>${esc(f.week)}: significant ${f.kind} (${f.tier}) — tap to open this week</title></text></g>`}).join("");return `<div class="ribbon-wrap"><svg viewBox="0 0 ${W} ${H}" role="img" aria-label="Stance mix over 26 weeks with confidence band and inflection markers" preserveAspectRatio="none" style="width:100%;height:130px;display:block">
<polygon points="0,${H} ${posPts} ${W},${H}" fill="var(--blue)" opacity=".5"/><polygon points="0,0 ${negPts} ${W},0" fill="var(--red)" opacity=".5"/>${ci}${low}${marks}</svg><div class="ribbon-legend">Narrative mix: blue rising from below = positive share, red hanging from above = negative share, paper between = neutral. Shaded band: 95% confidence on the positive share. Faded columns: too little data to trust. Markers are significance crossings — ▲ surge ⇄ stance shift ▼ fade.</div></div>`}
const FUNNEL_STAGE_LABELS={enrolled:"Enrolled",catalogued:"Catalogued",transcript_attempted:"Transcript attempted",segmented:"Segmented",intelligence_ready:"Intelligence-ready"};
function funnelStageCard(stage,index,stages){const first=Math.max(stages[0]?.episodes||1,1),prev=Math.max(stages[index-1]?.episodes||first,1),width=Math.max(40,Math.round(Math.sqrt(stage.episodes/first)*100)),conversion=index?stage.episodes/prev:1;return `<article class="funnel-stage" style="width:${width}%"><div class="funnel-stage-top"><div><div class="eyebrow">Stage ${index+1}</div><h3>${esc(stage.label)}</h3></div></div><div class="funnel-stage-metrics"><span><b>${stage.shows.toLocaleString()}</b><small>shows</small></span><span><b>${stage.episodes.toLocaleString()}</b><small>episodes</small></span><span><b>${stage.segments.toLocaleString()}</b><small>segments</small></span></div><span class="meta">${index?`${Math.round(conversion*100)}% of previous episode stage`:"complete feed inventory"}</span><div class="funnel-conversion" aria-label="${Math.round(conversion*100)} percent of previous episode stage"><i style="width:${Math.min(100,Math.round(conversion*100))}%"></i></div></article>`}
function funnelShowCard(show){const rss=safeUrl(show.rss_url),category=String(show.category||"uncategorized").replace(/_/g," "),stage=FUNNEL_STAGE_LABELS[show.stage]||cap(show.stage);return `<article class="show-row"><div class="show-row-top"><div><h3>${esc(show.name)}</h3><div class="pill-row"><span class="pill">${esc(category)}</span><span class="pill ${show.stage==="intelligence_ready"?"trust":""}">${esc(stage)}</span>${show.transcript_quarantined?`<span class="pill shift">${show.transcript_quarantined} quarantined</span>`:""}</div></div>${rss?`<a href="${esc(rss)}" target="_blank" rel="noopener">Open RSS feed</a>`:""}</div><div class="show-counts">${show.catalogued.toLocaleString()} catalogued episodes · ${show.transcript_attempted.toLocaleString()} transcript attempts · ${show.segmented.toLocaleString()} segmented episodes / ${(show.segments||0).toLocaleString()} segments · ${show.intelligence_ready.toLocaleString()} ready episodes / ${(show.intelligence_ready_segments||0).toLocaleString()} ready segments</div></article>`}
function enrollmentRequest(){const name=document.querySelector("#enroll-name")?.value.trim()||"",rss=document.querySelector("#enroll-rss")?.value.trim()||"",category=document.querySelector("#enroll-category")?.value.trim()||"uncategorized";const body=["## Podcast enrollment request","",`- Show: ${name}`,`- RSS feed: ${rss}`,`- Category: ${category}`,"- Policy: private_analysis_only","- Transcript policy: creator_rss_or_official_public_transcripts","","## Operator checklist","","- [ ] Verify the RSS feed and canonical show identity","- [ ] Check for an existing or legacy source alias","- [ ] Add to config/sources.yaml","- [ ] Run a bounded ingestion dry run before enqueueing work","","Created from Signal Desk Podcast Funnel."].join("\n");return{name,rss,category,body,title:`Enroll podcast: ${name}`}}
function wireFunnel(f){const shows=f.shows||[],search=document.querySelector("#show-search"),filter=document.querySelector("#stage-filter"),box=document.querySelector("#show-results"),count=document.querySelector("#show-count"),more=document.querySelector("#show-more");let showAll=false;const draw=()=>{const q=(search.value||"").toLowerCase(),stage=filter.value,matched=shows.filter(s=>(!q||`${s.name} ${s.category||""}`.toLowerCase().includes(q))&&(!stage||s.stage===stage)),visible=showAll?matched:matched.slice(0,12);box.innerHTML=visible.map(funnelShowCard).join("")||`<div class="empty">No enrolled shows match those filters.</div>`;count.textContent=`${matched.length} of ${shows.length} shows`;more.hidden=showAll||matched.length<=12;more.textContent=`Show all ${matched.length}`};search.oninput=()=>{showAll=false;draw()};filter.onchange=()=>{showAll=false;draw()};more.onclick=()=>{showAll=true;draw()};draw();const form=document.querySelector("#enroll-form"),note=document.querySelector("#enroll-note");form.onsubmit=e=>{e.preventDefault();const request=enrollmentRequest();if(!safeUrl(request.rss)){note.textContent="Enter a complete http or https RSS URL.";return}const url=`https://github.com/kolbydayley/pif-factory/issues/new?title=${encodeURIComponent(request.title)}&body=${encodeURIComponent(request.body)}`;window.open(url,"_blank","noopener");note.textContent="Opened a private enrollment request. The show enters the funnel after feed verification."};document.querySelector("#copy-enroll").onclick=()=>{const request=enrollmentRequest();if(!request.name||!safeUrl(request.rss)){note.textContent="Add a show name and complete RSS URL first.";return}(navigator.clipboard?navigator.clipboard.writeText(request.body):Promise.reject()).then(()=>note.textContent="Enrollment request copied.").catch(()=>note.textContent="Copy failed; use the private request button instead.")}}
function clearFunnelRefresh(){if(funnelRefreshTimer){clearTimeout(funnelRefreshTimer);funnelRefreshTimer=null}}
function renderFunnelView(payload){const f=payload.funnel,updated=payload.generated_at?new Date(payload.generated_at).toLocaleString():"server snapshot";app.innerHTML=`<section class="view"><div class="home-head"><div><div class="eyebrow">Podcast operations</div><h1>Podcast Funnel</h1><p class="lede">See how enrolled shows and episodes move from feed inventory into transcript processing and intelligence-ready evidence.</p></div><div class="status" aria-live="polite">Live server data · ${esc(updated)}</div></div><div class="funnel-grid" aria-label="Podcast processing funnel">${f.stages.map(funnelStageCard).join("")}</div><div class="callout">Transcript attempted includes ${f.quarantined_transcript_episodes.toLocaleString()} quarantined episodes. Segmented and intelligence-ready stages show the material that actually moved forward.</div><div class="research-grid funnel-layout"><section class="panel"><div class="section-head" style="margin-top:0"><div><div class="eyebrow">Current enrollment</div><h2>All shows</h2></div><p id="show-count"></p></div><div class="roster-controls"><input class="search" id="show-search" aria-label="Search enrolled shows" placeholder="Search shows or categories"><select class="roster-select" id="stage-filter" aria-label="Filter shows by stage"><option value="">All stages</option><option value="intelligence_ready">Intelligence-ready</option><option value="segmented">Segmented</option><option value="transcript_attempted">Transcript attempted</option><option value="catalogued">Catalogued only</option><option value="enrolled">No episodes yet</option></select></div><div class="show-list" id="show-results"></div><button class="secondary" id="show-more" type="button" style="width:100%;margin-top:12px">Show all</button></section><aside><section class="panel"><div class="eyebrow">Source intake</div><h2 class="panel-title">Add a new show</h2><p class="panel-sub">Signal Desk is public, so this creates a private enrollment request rather than writing anonymously into the production corpus. Feed verification remains the gate.</p><form class="enroll-form" id="enroll-form"><div class="form-field"><label for="enroll-name">Show name</label><input id="enroll-name" required autocomplete="off" placeholder="Podcast name"></div><div class="form-field"><label for="enroll-rss">RSS feed URL</label><input id="enroll-rss" required type="url" inputmode="url" placeholder="https://example.com/feed.xml"></div><div class="form-field"><label for="enroll-category">Category</label><input id="enroll-category" autocomplete="off" placeholder="frontier_ai"></div><div class="form-actions"><button class="primary" type="submit">Open private enrollment request</button><button class="secondary" id="copy-enroll" type="button">Copy enrollment request</button></div><div class="enroll-note" id="enroll-note" aria-live="polite"></div></form></section></aside></div>${coverageNote()}</section>`;wireFunnel(f)}
async function refreshFunnel(initial=false){const token=++funnelLoadToken;try{const url=new URL(FUNNEL_DATA_URL);url.searchParams.set("refresh",Date.now());const response=await fetch(url,{cache:"no-store",headers:{accept:"application/json"}});if(!response.ok)throw new Error(`server returned ${response.status}`);const payload=await response.json();if(!payload?.funnel?.stages||!Array.isArray(payload.funnel.shows))throw new Error("invalid funnel data");if(token!==funnelLoadToken||parseRoute().kind!=="funnel")return;const changed=!liveFunnelPayload||payload.generated_at!==liveFunnelPayload.generated_at;liveFunnelPayload=payload;if(initial||changed)renderFunnelView(payload)}catch(error){if(token!==funnelLoadToken||parseRoute().kind!=="funnel")return;if(!liveFunnelPayload)app.innerHTML=`<section class="view"><div class="eyebrow">Podcast operations</div><h1>Funnel data is unavailable.</h1><p class="lede">Signal Desk could not retrieve current processing counts from the server.</p><div class="button-row"><button class="primary" id="retry-funnel">Try again</button></div></section>`,document.querySelector("#retry-funnel").onclick=()=>renderFunnel();else{const status=document.querySelector(".status");if(status)status.textContent="Server refresh failed · showing last fetched snapshot"}}finally{if(token===funnelLoadToken&&parseRoute().kind==="funnel")funnelRefreshTimer=setTimeout(()=>refreshFunnel(false),60000)}}
function renderFunnel(){navState("funnel");clearFunnelRefresh();if(liveFunnelPayload)renderFunnelView(liveFunnelPayload);else app.innerHTML=`<section class="view"><div class="eyebrow">Podcast operations</div><h1>Loading the funnel…</h1><p class="lede">Fetching current show, episode, and segment counts from the server.</p></section>`;refreshFunnel(true)}
function navState(tab){document.querySelectorAll(".nav-link").forEach(b=>b.classList.toggle("active",(b.dataset.nav||b.dataset.id)===tab))}function coverageNote(){const gap=DATA.latest_episode?Math.max(0,Math.round((new Date(DATA.latest_episode)-new Date(DATA.data_through))/864e5)):null;return `<div class="coverage">Labels through ${esc(DATA.data_through)}${DATA.latest_episode?` · episodes through ${esc(DATA.latest_episode)}${gap>0?` · ${gap} days behind`:""}`:""} · ${DATA.corpus.labels.toLocaleString()} labeled segments · ${DATA.corpus.episodes.toLocaleString()} episodes · ${DATA.corpus.shows} shows. Trend windows follow the corpus frontier, not the calendar.</div>`}
function renderHome(tab){tab=["shifts","people","topics"].includes(tab)?tab:"shifts";navState(tab);const intro=`<div class="home-head"><div><div class="eyebrow">Research the discourse</div><h1>${tab==="shifts"?"What changed?":tab==="people"?"Who is shaping it?":"What is being discussed?"}</h1><p class="lede">${tab==="shifts"?"Start with movements that have real breadth, then open the issues, voices, evidence, and sources behind them.":tab==="people"?"Open a person to see recurring claims, changes of mind, disagreements, and source-grounded evidence.":"Browse the corpus vocabulary, then open any topic for related issues, stance differences, trusted voices, and evidence."}</p></div>${freshness()>21?`<div class="status">Coverage note: newest well-covered week is ${esc(DATA.data_through)}</div>`:""}</div>`;if(tab==="shifts"){app.innerHTML=`<section class="view">${intro}${askBox("")}${diffLog()}<div class="signal-grid">${signalCards().map(({kind,item})=>{const d=DATA.topics[item.topic],m=KIND[kind];return `<button class="signal-card ${item.tier||""}" data-route="topic" data-id="${esc(item.topic)}"><span class="signal-kind" style="color:${m.color}">${m.label}${item.tier?`<span class="tier-badge ${item.tier}">${item.tier}</span>`:""}</span><h3>${esc(tTitle(item.topic))}</h3><p>${esc(signalCopy(kind,item))}</p><span class="card-foot"><span class="explore">Explore the shift</span>${spark(d.series,m.color)}</span></button>`}).join("")}</div>${coverageNote()}</section>`}else if(tab==="people"){const ranked=[...DATA.people];app.innerHTML=`<section class="view">${intro}<button class="filter" data-route="network" style="margin-bottom:12px">Open the network map</button><input class="search" id="search" aria-label="Search people" placeholder="Search people or shows"><div class="list" id="results">${ranked.slice(0,40).map(personCard).join("")}</div>${coverageNote()}</section>`;wireSearch("people",ranked)}else{const topics=Object.entries(DATA.topics).filter(([t])=>t!=="other").sort((a,b)=>b[1].pulse_vol-a[1].pulse_vol||b[1].total-a[1].total);app.innerHTML=`<section class="view">${intro}<input class="search" id="search" aria-label="Search topics" placeholder="Search topics"><div class="list" id="results">${topics.slice(0,60).map(topicCard).join("")}</div>${coverageNote()}</section>`;wireSearch("topics",topics)}wireAsk()}
function personCard(p){const top=p.top_topics?.[0];return `<button class="person-card" data-route="person" data-id="${esc(p.name)}"><span class="row-top"><span class="row-title">${esc(p.name)}</span><span class="meta">${p.n_episodes} episodes</span></span><span class="pill-row">${p.trust?`<span class="pill trust" title="Network trust ${p.trust.score}/100 via ${esc(p.trust.basis)} — ${p.trust.n_links} co-appearance links. Connectivity, not correctness.">${esc(cap(p.trust.tier))} · net ${Math.round(p.trust.score)}</span>`:""}${p.authority?`<span class="pill trust">Authority ${p.authority.toFixed(2)}</span>`:""}${p.moves.length?`<span class="pill shift">Changed position</span>`:""}${top?`<span class="pill">Often on ${esc(top.topic)}</span>`:""}</span><span class="meta">${esc(p.shows.slice(0,4).join(" · "))}</span></button>`}function topicCard([name,d]){return `<button class="topic-card" data-route="topic" data-id="${esc(name)}"><span class="row-top"><span class="row-title">${esc(tTitle(name))}</span><span class="meta">${d.total} mentions</span></span><span class="pill-row"><span class="pill">${d.pulse_shows||0} recent shows</span><span class="pill">${d.evidence?.length||0} excerpts</span><span class="pill">${d.related?.length||0} related issues</span></span></button>`}
function wireSearch(kind,data){const input=document.querySelector("#search"),box=document.querySelector("#results");input.oninput=()=>{const q=input.value.toLowerCase();if(kind==="people")box.innerHTML=data.filter(p=>p.name.toLowerCase().includes(q)||p.shows.join(" ").toLowerCase().includes(q)).slice(0,50).map(personCard).join("");else box.innerHTML=data.filter(([n])=>n.includes(q)).slice(0,60).map(topicCard).join("")}}
function topicSignal(name){const hit=detectorKind.get(name);return hit?{...KIND[hit.kind],kind:hit.kind,item:hit.item}:{...KIND.discussed,kind:"discussed"}}function trend(topic,d,activeWeek){const mx=Math.max(...d.series.map(x=>x.share_smooth??x.share??0),.0001);return `<div class="trend-scroll"><div class="trend" aria-label="Weekly share of discourse and stance mix">${d.series.map((s,i)=>{const share=s.share_smooth??s.share??0,h=Math.max(2,Math.round(share/mx*124)),den=Math.max(s.vol,1),label=`${s.week}: ${s.vol} mentions · ${(share*100).toFixed(1)}% share of discourse${s.low_sample?" · low sample":""}`;return `<button class="week-bar ${s.low_sample?"low-sample ":""}${activeWeek===s.week?"active":""}" data-route="topic" data-id="${esc(topic)}" data-tail="week/${esc(s.week)}" aria-label="${esc(label)}" title="${esc(label)}"><span class="bar" style="height:${h}px"><i style="height:${s.neg/den*100}%;background:var(--red)"></i><i style="height:${s.neu/den*100}%;background:var(--neutral)"></i><i style="height:${s.pos/den*100}%;background:var(--blue)"></i></span>${i%5===0?`<span class="week-label">${s.week.slice(5)}</span>`:""}</button>`}).join("")}</div></div>`}
function evidenceCard(e,topic,compact=false){const personExists=peopleByName.has(e.person),url=safeUrl(e.source_url);return `<article class="evidence"><div class="evidence-top"><div>${personExists?`<button class="person-link" data-route="person" data-id="${esc(e.person)}">${esc(e.person)}</button>`:`<strong>${esc(e.person)}</strong>`}<div class="meta">${e.authority_scored?`Authority ${e.authority.toFixed(2)} · `:"Unscored voice · "}${esc(e.show)} · ${esc(e.date)}</div></div><span class="stance ${esc(e.group)}">${esc(e.stance||e.group)}</span></div><p class="quote">“${esc(e.evidence)}”</p><div class="source-line"><span class="source-title">${esc(e.episode)}</span><span class="source-actions">${!compact?`<button class="back" data-route="evidence" data-id="${esc(e.id)}" data-tail="${esc(topic)}">Read context</button>`:""}${url?`<a href="${esc(url)}" target="_blank" rel="noopener">Original source</a>`:""}</span></div></article>`}
function renderTopic(name,filterType,filterValue){const d=DATA.topics[name];if(!d){renderNotFound();return}navState("");const sig=topicSignal(name);let evidence=d.evidence||[];if(filterType==="stance")evidence=evidence.filter(e=>e.group===filterValue);if(filterType==="week")evidence=evidence.filter(e=>e.week===filterValue);const trusted=(d.evidence||[]).filter(e=>e.authority_scored).slice(0,6),activeLabel=filterType?`${cap(filterType)}: ${filterValue}`:"All evidence";app.innerHTML=`<section class="view"><button class="back" data-route="home" data-id="shifts">Back to shifts</button><div class="detail-head"><div><div class="eyebrow detail-label" style="color:${sig.color}">${sig.label}${sig.item?.tier?`<span class="tier-badge ${sig.item.tier}">${sig.item.tier} signal</span>`:""}</div><h1>${esc(name)}</h1><p class="lede">${esc(signalCopy(sig.kind,sig.item||{pulse_vol:d.pulse_vol,shows:d.pulse_shows,episodes:d.pulse_episodes}))}</p></div></div><div class="metric-grid"><div class="metric"><b>${d.total}</b><span>mentions in 26 weeks</span></div><div class="metric"><b>${d.pulse_episodes||0}</b><span>recent episodes</span></div><div class="metric"><b>${d.pulse_shows||0}</b><span>recent shows</span></div></div><div class="research-grid topic-layout"><div><section class="panel"><h2 class="panel-title">How the conversation moved</h2>${ribbon(name,d)}${shiftCompare(sig,d)}<p class="panel-sub">Height is three-week smoothed share of discourse. Tap a week for exact mentions and evidence; dim bars have low coverage.</p>${trend(name,d,filterType==="week"?filterValue:"")}<div class="filter-row"><button class="filter ${!filterType?"active":""}" data-route="topic" data-id="${esc(name)}">All</button>${["positive","negative","neutral"].map(g=>`<button class="filter ${filterType==="stance"&&filterValue===g?"active":""}" data-route="topic" data-id="${esc(name)}" data-tail="stance/${g}">${cap(g)} ${d.evidence_stances?.[g]||0}</button>`).join("")}</div></section><section class="panel"><h2 class="panel-title">Evidence and sources</h2><p class="panel-sub">${esc(activeLabel)} · short excerpts only, linked to original material.</p><div class="evidence-list">${evidence.length?evidence.map(e=>evidenceCard(e,name)).join(""):`<div class="empty">No quotable evidence is attached to this slice yet. Try another stance or return to all evidence.</div>`}</div></section></div><aside><section class="panel"><h2 class="panel-title">Major issues at play</h2><p class="panel-sub">Topics that repeatedly appear in the same episodes.</p><div class="issue-grid">${d.related?.length?d.related.map(r=>`<button class="issue-card" data-route="topic" data-id="${esc(r.topic)}"><b>${esc(r.topic)}</b><span>${r.shared_episodes} shared episodes · ${r.shared_shows} shows</span></button>`).join(""):`<div class="empty">No related issue cluster yet.</div>`}</div></section><section class="panel"><h2 class="panel-title">Most trusted voices</h2><p class="panel-sub">Authority-scored people with evidence on this topic.</p>${trusted.length?`<div class="evidence-list">${trusted.map(e=>evidenceCard(e,name,true)).join("")}</div>`:`<div class="callout">No authority-scored person is attached to this topic yet. Source-grounded evidence remains available without a trust claim.</div>`}</section></aside></div>${coverageNote()}</section>`}
function claimAttrs(t){return DATA.topics[t]?`data-route="topic" data-id="${esc(t)}"`:`disabled aria-disabled="true" title="Not enough corpus volume for a topic page yet" style="cursor:default;opacity:.7"`}
function renderPerson(name){const p=peopleByName.get(name);if(!p){renderNotFound();return}navState("");app.innerHTML=`<section class="view"><button class="back" data-route="home" data-id="people">Back to people</button><div class="detail-head"><div><div class="eyebrow detail-label">Person</div><h1>${esc(name)}</h1><p class="lede">${p.authority?`Authority score ${p.authority.toFixed(2)} · `:"No authority score · "}${p.n_episodes} episodes across ${p.shows.length} shows.</p></div></div><div class="research-grid"><div><section class="panel"><h2 class="panel-title">Biggest recurring claims</h2><p class="panel-sub">Topics ranked by how often this person takes a recorded position.</p><div class="claim-list">${p.top_topics?.length?p.top_topics.map(t=>{const total=Math.max(t.count,1);return `<button class="claim" ${claimAttrs(t.topic)}><b>${esc(t.topic)}</b><span class="meta">${t.count} positions</span><span class="stance-line"><i style="width:${t.positive/total*100}%;background:var(--blue)"></i><i style="width:${t.neutral/total*100}%;background:var(--neutral)"></i><i style="width:${t.negative/total*100}%;background:var(--red)"></i></span></button>`}).join(""):`<div class="empty">No topic summary yet.</div>`}</div></section><section class="panel"><h2 class="panel-title">Evidence record</h2><p class="panel-sub">Recent source-grounded excerpts. Open any topic or original source for more context.</p><div class="evidence-list">${p.evidence?.length?p.evidence.map(e=>evidenceCard({...e,person:p.name,authority:p.authority,authority_scored:Boolean(p.authority)},e.topic)).join(""):`<div class="empty">No quotable evidence attached.</div>`}</div></section></div><aside>${p.moves.length?`<section class="panel"><h2 class="panel-title">Changed positions</h2><div class="claim-list">${p.moves.slice().reverse().map(m=>`<button class="claim" ${claimAttrs(m.topic)}><b>${esc(m.topic)}</b><span class="meta">${cap(m.from)} to ${cap(m.to)} · ${m.from_date} to ${m.to_date}</span></button>`).join("")}</div></section>`:""}${p.against_field.length?`<section class="panel"><h2 class="panel-title">Against the field</h2><div class="claim-list">${p.against_field.map(a=>`<button class="claim" ${claimAttrs(a.topic)}><b>${esc(a.topic)}</b><span class="meta">${cap(a.stance)} while ${Math.round(a.majority_share*100)}% of the field is ${a.field_majority}</span></button>`).join("")}</div></section>`:""}<section class="panel"><h2 class="panel-title">Appears on</h2><p>${esc(p.shows.join(" · "))}</p></section></aside></div>${coverageNote()}</section>`}
function findEvidence(id){for(const [topic,d] of Object.entries(DATA.topics))for(const e of d.evidence||[])if(e.id===id)return{topic,e};for(const p of DATA.people)for(const e of p.evidence||[])if(e.id===id)return{topic:e.topic,e:{...e,person:p.name,authority:p.authority,authority_scored:Boolean(p.authority)}};return null}function episodeSiblings(episodeId,excludeId){const out=[];for(const [tname,d] of Object.entries(DATA.topics)){for(const ev of (d.evidence||[])){if(ev.episode_id===episodeId&&ev.id!==excludeId&&!out.some(x=>x.e.id===ev.id))out.push({topic:tname,e:ev})}}return out.slice(0,6)}
function renderEvidence(id,topicHint){const hit=findEvidence(id);if(!hit){renderNotFound();return}const {topic,e}=hit,url=safeUrl(e.source_url),personExists=peopleByName.has(e.person);const sibs=episodeSiblings(e.episode_id,e.id);navState("");app.innerHTML=`<section class="view evidence-page"><button class="back" data-route="topic" data-id="${esc(topicHint||topic)}">Back to topic</button><div class="eyebrow detail-label">Evidence context</div><h1 class="title-case">${esc(topic)}</h1><div class="ctx-where"><b>${esc(e.show)}</b> · ${esc(e.episode)} · ${esc(e.date)}${e.claim_type?` · ${esc(e.claim_type)}`:""} · stated by ${esc(e.person)} (${esc(e.stance||e.group)})</div><div class="ctx-quote" id="ctxq">${e.context_before?`<span class="ctx-dim"><span class="ctx-short">…${esc(tailW(e.context_before,280))}</span><span class="ctx-long">…${esc(e.context_before)}</span> </span>`:""}<mark class="ctx-mark">${esc(e.evidence)}</mark>${e.context_after?`<span class="ctx-dim"> <span class="ctx-short">${esc(headW(e.context_after,280))}…</span><span class="ctx-long">${esc(e.context_after)}…</span></span>`:""}</div>${(e.context_before||"").length>300||(e.context_after||"").length>300?`<button class="filter ctx-toggle" onclick="toggleCtx()">Show more context</button>`:""}${e.context_before||e.context_after?`<div class="ribbon-legend">Highlighted text is the extracted evidence; dimmed text is the surrounding transcript window (bounded excerpt).</div>`:`<div class="ribbon-legend">No transcript window is available for this excerpt yet.</div>`}${sibs.length?`<section class="panel"><h2 class="panel-title">Also from this episode</h2><div class="evidence-list">${sibs.map(({topic:t,e:ev})=>evidenceCard(ev,t,true)).join("")}</div></section>`:""}<div class="source-card"><div class="evidence-top"><div>${personExists?`<button class="person-link" data-route="person" data-id="${esc(e.person)}">${esc(e.person)}</button>`:`<strong>${esc(e.person)}</strong>`}<div class="meta">${e.authority_scored?`Authority ${e.authority.toFixed(2)} · `:"Unscored voice · "}${esc(e.date)}</div></div><span class="stance ${esc(e.group)}">${esc(e.stance||e.group)}</span></div><h3 style="margin-top:18px">${esc(e.episode)}</h3><p class="meta" style="margin-top:5px">${esc(e.show)} · confidence ${Math.round((e.confidence||0)*100)}%</p><div class="button-row"><button class="secondary" data-route="topic" data-id="${esc(topic)}">Explore this topic</button>${personExists?`<button class="secondary" data-route="person" data-id="${esc(e.person)}">View this person</button>`:""}${url?`<a class="primary" href="${esc(url)}" target="_blank" rel="noopener">Open original source</a>`:""}</div></div></section>`}
function renderNotFound(){app.innerHTML=`<section class="view"><h1>That view is unavailable.</h1><p class="lede">The nightly data may have changed.</p><div class="button-row"><button class="primary" data-route="home" data-id="shifts">Return to shifts</button></div></section>`}function render(){const r=parseRoute();if(r.kind!=="funnel"){clearFunnelRefresh();funnelLoadToken++}if(r.kind==="home")renderHome(r.id);else if(r.kind==="network")renderNetwork();else if(r.kind==="funnel")renderFunnel();else if(r.kind==="ask")renderAsk(r.id);else if(r.kind==="topic")renderTopic(r.id,r.filterType,r.filterValue);else if(r.kind==="person")renderPerson(r.id);else if(r.kind==="evidence")renderEvidence(r.id,r.filterType);else renderNotFound();app.focus({preventScroll:true});window.scrollTo(0,0)}document.addEventListener("click",e=>{const target=e.target.closest("[data-route]");if(!target)return;const tail=(target.dataset.tail||"").split("/").filter(Boolean);route(target.dataset.route,target.dataset.id||"",tail)});window.addEventListener("hashchange",render);if(!location.hash)location.hash="home/shifts";else render();
</script></body></html>"""


def _last_share(topic_info):
    smooth = [s.get("share_smooth") for s in topic_info.get("series", [])
              if s.get("vol")]
    return smooth[-1] if smooth else None


def compute_diff(cur, prev):
    """What changed since the previous build — the weekly deep read's
    opening section. None when there is no previous build to diff."""
    if not prev:
        return None
    diff = {"since": prev.get("generated_at", "")[:10],
            "detectors": [], "movers": [], "new_people": []}
    for family, entries in cur.get("detectors", {}).items():
        cur_map = {e["topic"]: e for e in entries}
        prev_map = {e["topic"]: e
                    for e in prev.get("detectors", {}).get(family, [])}
        for topic, e in cur_map.items():
            if topic not in prev_map:
                diff["detectors"].append(
                    {"topic": topic, "family": family, "change": "new",
                     "tier": e.get("tier")})
            elif e.get("tier") != prev_map[topic].get("tier"):
                diff["detectors"].append(
                    {"topic": topic, "family": family, "change": "tier",
                     "from": prev_map[topic].get("tier"),
                     "to": e.get("tier")})
        for topic in prev_map:
            if topic not in cur_map:
                diff["detectors"].append(
                    {"topic": topic, "family": family, "change": "gone"})
    movers = []
    for topic, info in cur.get("topics", {}).items():
        prev_info = prev.get("topics", {}).get(topic)
        if not prev_info:
            continue
        c, p = _last_share(info), _last_share(prev_info)
        if c is None or p is None:
            continue
        if abs(c - p) >= 0.005:
            movers.append({"topic": topic, "delta": round(c - p, 4)})
    movers.sort(key=lambda m: -abs(m["delta"]))
    diff["movers"] = movers[:6]
    prev_names = {p.get("name") for p in prev.get("people", [])}
    diff["new_people"] = [p["name"] for p in cur.get("people", [])
                          if p.get("name") not in prev_names][:8]
    return diff


def main() -> None:
    data = json.loads(DATA.read_text())
    prev_path = DATA.with_name("data_prev.json")
    prev = None
    if prev_path.exists():
        try:
            prev = json.loads(prev_path.read_text())
        except json.JSONDecodeError:
            prev = None
    data["diff"] = compute_diff(data, prev)
    funnel_payload = {
        "schema_version": data.get("schema_version"),
        "generated_at": data.get("generated_at"),
        "data_through": data.get("data_through"),
        "latest_episode": data.get("latest_episode"),
        "funnel": data["funnel"],
    }
    FUNNEL_OUT.write_text(json.dumps(funnel_payload, separators=(",", ":")))
    embedded_data = dict(data)
    embedded_data.pop("funnel", None)
    OUT.write_text(TEMPLATE.replace("__DATA__", json.dumps(embedded_data)))
    print(json.dumps({"out": str(OUT), "bytes": OUT.stat().st_size,
                      "funnel_out": str(FUNNEL_OUT),
                      "funnel_bytes": FUNNEL_OUT.stat().st_size}))


if __name__ == "__main__":
    main()
